import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF6EA07A);
  static const Color primaryDark = Color(0xFF5A8D68);
  static const Color primaryLight = Color(0xFF8CB99A);

  // Background Colors
  static const Color background = Color(0xFFF7F7F7);
  static const Color backgroundDark = Colors.black;
  static const Color cardBackground = Colors.white;
  static const Color cardBackgroundDark = Color(0xFF121212);

  // Text Colors
  static const Color textPrimary = Colors.black;
  static const Color textSecondary = Colors.grey;
  static const Color textWhite = Colors.white;

  // Status Colors
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE57373);
  static const Color warning = Color(0xFFFF9800);
  static const Color info = Color(0xFF2196F3);

  // Accent Colors
  static const Color darkBlue = Color(0xFF071E7A);
  static const Color lightGrey = Color(0xFFE0E0E0);

  // Transparent
  static const Color transparent = Colors.transparent;
}
